package util

// Utility function to get the last value of a map
func GetLastValueOfMap(_map *map[string]int) int {
	var last_value int

	// Iterate over the map and get last value
	for _, last_value = range *_map {
	}

	return last_value
}
